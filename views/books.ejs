<!-- books.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>📚 Books Library</title>
    <link rel="stylesheet" href="/style/css/main.css">
    <link rel="stylesheet" href="/style/css/buttons.css">
    <link rel="stylesheet" href="/style/css/table.css">
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>📚 Books Library</h1>
            <div class="create-btn-container">
                <a href="/create" class="btn btn-create">➕ Create New Book</a>
            </div>
        </div>

        <% if (books.length === 0) { %>
            <div class="empty-state">
                <div class="book-icon">📚</div>
                <h3>No Books Yet</h3>
                <p>Start building your library by adding your first book!</p>
                <a href="/create" class="btn btn-create">Add Your First Book</a>
            </div>
        <% } else { %>
            <div class="table-wrapper">
                <table>
                    <thead>
                        <tr>
                            <th>📖 Title</th>
                            <th>✍️ Author</th>
                            <th>⚙️ Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for(var i=0; i < books.length; i++) { %>
                        <tr>
                            <td><strong><%= books[i].title %></strong></td>
                            <td><%= books[i].author %></td>
                            <td>
                                <div class="actions">
                                    <a href="/book/<%= books[i].id %>" class="btn btn-view">👁️ View</a>
                                    <a href="/update/<%= books[i].id %>" class="btn btn-edit">✏️ Edit</a>
                                    <a href="/delete/<%= books[i].id %>" class="btn btn-delete" onclick="return confirm('Are you sure you want to delete this book?')">🗑️ Delete</a>
                                </div>
                            </td>
                        </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        <% } %>
    </div>
</body>
</html>